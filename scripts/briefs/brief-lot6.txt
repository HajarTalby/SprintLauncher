Tu es un agent dev autonome (codex) sur le Sprint Launcher, une app .NET : CLI `src/SprintLauncher`, UI WPF `src/SprintLauncher.UI`, tests `src/SprintLauncher.Tests`. Tu es DÉJÀ dans ton worktree git isolé, sur ta branche dédiée `sl-lot6-gardien` (basée sur l'intégration `sl-fixes-2026-07-16`). Travaille UNIQUEMENT ici, ne touche pas à d'autres dossiers/branches, ne fais AUCUN push. Réponds en français.

TÂCHE — SERZENIA-144, Lot 6 « gardien du dépôt » (amélioration anti-récidive).
Contexte : pendant un run, les acteurs (ccode, codex) partagent le même arbre de travail et committent en concurrence → écarts (fichiers écrasés, artefacts non déterministes, état git incohérent). Fiabilise ça :
1. TOUR DE RÉCONCILIATION après chaque tour d'acteurs : `git add` ciblé des fichiers réellement produits, jamais de réécriture d'historique, détecte et SIGNALE les écarts au lieu de les écraser silencieusement.
2. 1 MOTEUR / 2 VERROUS : garantir qu'un même moteur (OAuth ccode ou codex) n'est pas utilisé par deux acteurs simultanément (verrou par moteur).
3. DÉTERMINISME DES ARTEFACTS : chemins, noms de fichiers et ordre de production reproductibles.

Explore d'abord : la boucle d'orchestration des acteurs et leurs commits dans `Program.cs`, les runners dans `Runners/`, la gestion des artefacts et de l'état (`SprintStateManager`, `state.json`). Vérifie ce qui existe déjà avant de coder, reste chirurgical.

CONTRAINTES : scripts `.ps1` strictement ASCII (PS 5.1 = Windows-1252). Compile les DEUX projets (`dotnet build src/SprintLauncher` ; `dotnet build src/SprintLauncher.UI`) et lance les tests (`dotnet test src/SprintLauncher.Tests`) — pas de « fini » sans build + tests verts, ajoute des tests. Aucun secret en clair.

LIVRABLE : commit(s) sur `sl-lot6-gardien`, message commençant par `SERZENIA-144 (Lot 6)`. Termine par un résumé : fichiers modifiés, ce qui est implémenté, résultat build+tests, points de décision/blocage.
